CREATE TABLE drinks(
                        id INT PRIMARY KEY,
                        name VARCHAR(255) NOT NULL,
                        last_name VARCHAR(255) NOT NULL,
                        presence INT NOT NULL

);